syntax = "proto3";

package barsam.cpg.messages;

import "cpg/models/cpgRequest.proto";

// Define the request message
message GetRequest {
  uint64 id = 1;
}

// Define the response message
message GetResponse {
  models.CPG cpg = 1;
}