syntax = "proto3";

package barsam.user.models;

import "user/models/field.proto";
import "user/models/field_option.proto";

message UserMeta {
  // The custom field that this meta field is for
  Field field = 1;

  FieldOption field_option = 2;

  // The value of the custom field
  string value = 3;


}