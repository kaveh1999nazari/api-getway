syntax = "proto3";

package barsam.user.messages;

// Define the request message
message UpdateRequest {
  // The user id
  uint64 id = 1;

  // Non-encrypted password
  string password_raw = 2;

  // Meta fields
  map<uint64, string> meta_fields = 3;
}

// Define the response message
message UpdateResponse {

}