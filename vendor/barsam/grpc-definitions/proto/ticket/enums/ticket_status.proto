syntax = "proto3";

package barsam.ticket.enums;

enum TicketStatus {
  UNKNOWN = 0;
  OPEN = 1;
  IN_PROGRESS = 2;
  PENDING = 3;
  CLOSED = 4;
  RESPOND = 5;
  ON_HOLD = 6;
  CANCELLED = 7;
  RESOLVED = 8;
}