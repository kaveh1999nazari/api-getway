syntax = "proto3";

package barsam.ticket.messages;

import "ticket/models/ticket.proto";

// Define the request message
message GetRequest {
  uint64 id = 1;
}

// Define the response message
message GetResponse {
  models.Ticket ticket = 1;
}