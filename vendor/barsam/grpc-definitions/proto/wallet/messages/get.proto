syntax = "proto3";

package barsam.wallet.messages;

import "wallet/models/wallet.proto";

// Define the request message
message GetRequest {
  uint64 id = 1;
  string address = 2;
}

// Define the response message
message GetResponse {
  models.Wallet wallet = 1;
}